import { NavBar } from "../../Components/NavBar"

export const Profile = ():JSX.Element => {
    return (
            <main>
                <NavBar/>
                <h1>Profile</h1>
            </main>
    )
}